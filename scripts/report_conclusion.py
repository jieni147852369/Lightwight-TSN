#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""在文档末尾追加测试结论与建议章节。"""
import docx

DOC = "轻量时间敏感网络测试报告.docx"
d = docx.Document(DOC)


def add(level, text):
    p = d.add_paragraph(text)
    p.style = d.styles["Normal"] if level == 0 else d.styles[f"Heading {level}"]


TAIL = [
    (1, "7 测试结果汇总"),
    (0, "本次测试覆盖 TAS 门控调度、CBS 队列限速、端到端通信时延与 gPTP 时间同步四类能力，"
        "共执行用例：TAS 门控 2 项、CBS 限速与门控 6 项、通信时延 1 项、时间同步 3 项。"),
    (2, "7.1 功能测试结果"),
    (0, "TAS 用例中，门控全开时高优先级流量优先被接收；先关后开使各优先级流量同时到达再统一放行时，"
        "仍按优先级由高到低依次调度，结果符合预期。CBS 用例中，无论低速单线程（304K）还是高速多线程（53M）场景，"
        "限速后各队列发送速率均符合预先配置；门控由低到高依次开启时接收端顺序符合预期。"),
    (2, "7.2 性能测试结果"),
    (0, "端到端通信时延在门控全开、单帧点对点发送场景下满足设计要求；发送端采用绝对时间调度并对同刻多帧"
        "按优先级有序发送，接收端基于硬件/软件时间戳统计单向时延，测量链路完整、数据可复现。"),
    (2, "7.3 时间同步测试结果"),
    (0, "gPTP 主/备/从三节点在启动后完成同步；主时钟主动调快 5s 后，备用与从节点时钟均跟随收敛；"
        "角色切换测试中，关闭主节点后备用节点升主、从节点继续跟随，再关闭备用节点后从节点行为符合预期，"
        "验证了主备从选举与同步跟随机制。"),

    (1, "8 结论与建议"),
    (2, "8.1 测试结论"),
    (0, "被测轻量时间敏感网络软件的 TAS、CBS、通信时延与 gPTP 时间同步核心功能均通过测试，实际结果与设计目标一致，"
        "满足验收要求。"),
    (2, "8.2 后续建议"),
    (0, "建议补充长时间稳定性（老化）测试、更大流量并发下的抖动统计，以及与第三方 TSN 交换机的互操作验证；"
        "同时对硬件时间戳不可用时的软件时间戳回退路径进行边界测试，进一步完善可观测性指标。"),
]

for lvl, txt in TAIL:
    add(lvl, txt)

d.save(DOC)
print("结论章节已追加，段落数：", len(d.paragraphs))